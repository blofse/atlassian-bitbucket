[Unit]
Description=Docker - Mysql - Atlassian Bitbucket container
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a atlassian-bitbucket-mysql
ExecStop=/usr/bin/docker stop -t 2 atlassian-bitbucket-mysql

[Install]
WantedBy=default.target
